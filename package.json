{
  "name": "square1",
  "version": "0.0.1",
  "description": "Lead tracking and resource management system.",
  "main": "build/app.js",
  "scripts": {
    "build:server": "babel -d ./build ./server --presets es2015,stage-2",
    "win:clean": "rmdir /s /q build && mkdir build",
    "win:copy": "xcopy server\\config build\\config /E /i",
    "win:build": "npm run win:clean && npm run build:server && npm run win:copy",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "nodemon ./server/app.js --exec babel-node --presets es2015,stage-2",
    "start": "node ./build/app.js"
  },
  "engines": {
    "node": "6",
    "npm": "*"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/wolfspace/square1"
  },
  "author": "Mike Rourke",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.15.2",
    "express": "^4.14.0",
    "morgan": "^1.7.0",
    "pg": "^6.1.2",
    "pg-hstore": "^2.3.2",
    "sequelize": "^3.28.0"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-stage-2": "^6.18.0",
    "nodemon": "^1.11.0"
  }
}
